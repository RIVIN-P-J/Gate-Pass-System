const PDFDocument = require('pdfkit'); const fs = require('fs'); const path = require('path'); const out=path.join(process.cwd(),'Gatepass_Project_Documentation.pdf'); const doc=new PDFDocument({margin:50,size:'A4'}); const stream=fs.createWriteStream(out); doc.pipe(stream); function title(t){doc.fontSize(20).text(t,{underline:true}); doc.moveDown();} function heading(t){doc.fontSize(14).text(t); doc.moveDown(0.3);} function para(t){doc.fontSize(11).text(t,{lineGap:4}); doc.moveDown();} function list(items){items.forEach(i=>{doc.fontSize(11).text('• '+i,{indent:10, lineGap:4});}); doc.moveDown();} title('College Gatepass Management System'); heading('Abstract'); para('This project is a campus gatepass management platform built with React/Vite frontend and Node.js/Express backend using MySQL. It enables students to request gatepasses, admins to approve or reject requests, security staff to verify exits and entries, and parents to receive real-time notifications via SMS and email. The solution supports role-based workflows, QR verification, analytics, and communication automation.'); heading('Detailed Features'); heading('Core User and Role Features'); list(['Student: request gatepasses, view history, manage parent contacts','Admin: review and manage requests, approve/reject, view analytics','Security: verify gatepasses, log exit/entry, prevent invalid movements']); heading('Gatepass Workflow'); list(['Student submits gatepass request with reason, destination, return time, and notes','Admin reviews pending requests and changes status to approved or rejected','Approved gatepasses are available for security verification','Security logs campus exit and entry, updating student status appropriately']); heading('Parent Notification System'); list(['Automatic alerts on student exit and entry','SMS notifications via Twilio','Email notifications via Nodemailer','Parent contacts support SMS, email, preferred method, and active state']); heading('Real-time and Analytics Features'); list(['Socket-based real-time notifications for admins and students','Admin dashboard analytics for gatepass counts, late returns, and campus movement','Notifications for threshold events and overdue returns']); heading('Technical Requirements'); list(['Node.js and npm for backend/frontend development','MySQL database for persistent storage','Environment variables for DB, SMTP email, and Twilio SMS credentials','Frontend dependencies: React, Vite, Tailwind CSS, Framer Motion','Backend dependencies: Express, mysql2, jsonwebtoken, nodemailer, twilio, socket.io, zod']); heading('Related Project Components'); list(['backend/src/routes/auth.js: signup, login, and role management','backend/src/routes/gatepasses.js: student gatepass APIs','backend/src/routes/security.js: exit/entry verification and QR logic','backend/src/services/notificationService.js: email/SMS notification engine','backend/src/db/schema.sql: database schema definitions','frontend/src/pages/student/RequestGatepass.jsx: student gatepass form','frontend/src/components/ParentContactsManager.jsx: parent contact management','frontend/src/pages/admin/Requests.jsx & Analytics.jsx: admin dashboards']); heading('End-to-End Workflow'); para('1. Student signs up and completes profile registration.'); para('2. Student submits a gatepass request.'); para('3. Request is stored as pending until admin review.'); para('4. Admin approves or rejects the request.'); para('5. Student receives real-time update on approval status.'); para('6. Security verifies approved gatepasses and logs exits.'); para('7. System updates student status and sends parent notifications.'); para('8. Security logs entry when the student returns and notifies parents again.'); heading('Usage Summary'); para('This project streamlines campus gatepass processing, secures entry/exit operations, provides admin oversight, and keeps parents informed with automated notifications. It supports a modern campus workflow with real-time updates, analytics, and role-based access.'); doc.end(); stream.on('finish', ()=>console.log('PDF written'));